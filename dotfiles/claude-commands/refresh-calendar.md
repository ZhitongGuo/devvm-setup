# Refresh Calendar

1. Fetch today's meetings: `~/.claude/scripts/fetch-calendar.sh today`
2. Fetch this week's meetings: `~/.claude/scripts/fetch-calendar.sh week`
3. Update `~/my-vault/Calendar.md`
